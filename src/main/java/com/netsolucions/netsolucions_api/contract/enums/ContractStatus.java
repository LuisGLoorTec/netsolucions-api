package com.netsolucions.netsolucions_api.contract.enums;

public enum ContractStatus {
    ACTIVO,
    SUSPENDIDO,
    CANCELADO,
    FINALIZADO
}

// Se preguntaran que chingalos es un enum? pues un enum basicamente es una abrebiatura de enumeracion
// le decimos al codigo mira solo puedes ser Activo, Suspendido, etc etc namada mas carnal si
// alguien le dice que es otra cosa pues tira error, basicamente es un validador de estados
// asi manetnemos los errores humanos, basicamente es un escucdo anti pendejos va?
// uwu